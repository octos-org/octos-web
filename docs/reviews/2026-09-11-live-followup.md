# Mini3 live follow-up — 2026-09-11

The history-order fix passed a new uninterrupted **30m 33s / 38-cycle** soak on mini3. The same run checked full rendered user-turn order against the actual server transcript after every reload, gallery return and offline reconnection. Later extraction, citation-guidance and source-link fixes have separate deployment checks. Full feature acceptance remains incomplete.

Changes remain in open [web PR #350](https://github.com/octos-org/octos-web/pull/350) and [core PR #2296](https://github.com/octos-org/octos/pull/2296). This is the existing isolated mini3 instance at port 55080, not public-canary certification. Synthetic `review-owner` and `review-b` accounts performed new work. The user's `review-a` conversation was inspected without submitting messages or rewriting stored history.

| Validation | Runtime | Result |
|---|---|---|
| New remote soak | Web `ea8ce95`, core `7590b73`, PID 23885 | 38 cycles plus final real reply, 30m 33s, no retries/errors/observed HTTP failures |
| Core checks before soak | Same runtime | 5/5 in 17.3s |
| Switch sessions during an actual shell tool | Same runtime | Old foreground turn cancelled on connection close; new session preserved; queued text never sent into either conversation |
| Candidate weather extraction and source links | Web `ea8ce95`, core `b931bd2`, PID 27339 | Three actual model/tool cases passed; candidate profile setup failures retained separately |
| Core checks after backend deployment | Web `ea8ce95`, core `b931bd2`, PID 28157 | 5/5 in 17.0s |
| Original six-turn conversation after backend deployment | Same runtime | Chromium and WebKit each pass initial open and two reloads |
| Main-instance weather check after backend deployment | Same runtime | Failed: Chinese punctuation became part of a bare source URL; tracked as web #368 |

The [soak summary](evidence/2026-09-11-live/soak-summary.json), [per-cycle metrics](evidence/2026-09-11-live/soak-metrics.jsonl), [resource samples](evidence/2026-09-11-live/process-samples.jsonl), and [inspected synthetic screenshot](evidence/2026-09-11-live/soak-cycle-35.png) retain the evidence. Playwright 1.60.0 / Chromium 148.0.7778.96 ran on `macmini-3-249.local`; actual DeepSeek calls completed each turn. Test start was **2026-09-11 07:05:27.659 UTC**, final metric **07:36:01.292 UTC**. Median completion latency was **1.916s**, nearest-rank p95 **2.662s**. There were 214 WebSockets opened and 212 closed before browser teardown, leaving two active. Last cycle contained 38 user turns in exact transcript order.

Server RSS rose from **291,568 to 346,576 KiB** while histories grew and independent scope tests used the same server. Browser/runner summed RSS ranged **797,760–1,038,016 KiB** and ended at **910,960 KiB**. Those 46 samples cover 07:13:07–07:35:40 UTC, beginning after soak startup. Shared-page-inclusive process RSS does not exclude a memory leak.

The [scope result](evidence/2026-09-11-live/queue-scope.json) records a real `shell` start containing `sleep 12`, followed by the old turn's `errored / connection_closed` terminal 191ms later. An independent browser observed that terminal. New-session ownership, cancelled queue text and exactly one restored new reply all passed. Foreground work belongs to its submitting connection in the current transport; this result verifies cancellation isolation, not successful continuation of a foreground task after navigation. Earlier fixture attempts failed because an empty unsaved session was undiscoverable and because the first assertion incorrectly demanded completion rather than the actual cancellation contract.

[Core #2306](https://github.com/octos-org/octos/issues/2306) was exposed by real weather retrieval: plain-text extraction stripped tag delimiters but retained scripts/styles, crowding useful page text out of the output budget. Core `e7d9163` traverses the HTML5 DOM, omits non-content subtrees and comments, decodes entities correctly and separates blocks without splitting inline words. Markdown extraction preserves source links while excluding scripts/styles/templates/noscript. Both parser crates were already pinned transitive dependencies of the existing Markdown converter; no package version changed. Network/redirect/SSRF/allowlist and output caps are unchanged. Four new plain-text cases failed against the old implementation; **19 web-fetch tests** pass after the fix, including existing network restrictions.

Core `b931bd2` requires actual direct source links for web-based answers, distinguishes observation time from fetch time, preserves date/location qualifiers and requires separate evidence for heat causes or active warnings. **15 gateway prompt contract tests** and the embedded binary build pass. Backend CI [run 34573793760](https://github.com/octos-org/octos/actions/runs/34573793760) completed successfully. Persisted custom prompts remain respected; prompt guidance does not guarantee every sampled model response follows the format.

The [candidate weather results](evidence/2026-09-11-live/weather-candidate.json) contain three actual cases: explicit plain-text extraction, a natural Shanghai weather question and its Beijing follow-up. Every case performed a real lookup and produced an inline URL matching the tool evidence; the follow-up had no table or unrelated Saratoga comparison. The [manual source audit](evidence/2026-09-11-live/weather-source-audit.json) compares reported dates, temperatures, humidity and other inspected values with the **actual returned tool text**. A separate fetch returned an earlier Shanghai snapshot; the candidate's persisted tool result was stamped 15:15 and supports its answer. These checks establish support in those returned sources, not independent meteorological truth. Stale data, heat causes and active-warning expiry still need acceptance under [core #2305](https://github.com/octos-org/octos/issues/2305).

Candidate setup initially auto-started a standalone profile gateway, locking the database needed by the in-process runtime. Two attempts timed out before accepting a user row; the typed `data_dir_locked` diagnosis was retained. Disabling/stopping that candidate gateway and configuring its real provider resolved the fixture. After main deployment, initial launch commands referenced a nonexistent nested script directory and failed with `MODULE_NOT_FOUND` before testing; paths were corrected, and those logs remain private. These failures are not counted as passing runs.

The main weather check then revealed [web #368](https://github.com/octos-org/octos-web/issues/368): the model emitted a bare URL followed by `）。`, and GFM autolinking encoded that punctuation into the URL. [The failed answer and link observation](evidence/2026-09-11-live/weather-main-before-citation-fix.json) are retained. Web `39bbb1a` moves trailing CJK prose punctuation outside automatically detected bare links in chat and Home. Explicit Markdown/angle links, encoded URLs, ordinary parentheses and code are preserved. **Nine regressions fail before the change; all 14 source-link cases pass after it.** Full validation passes **1,273 unit tests**, **33 mocked browser tests**, production `/app/` build and lint with **zero errors / 105 warnings**.

Image-driven slide generation/edit rendering/PPTX export, remote ASR/TTS, complete learning lessons, external services and physical hardware remain outside acceptance. The earlier real slide task failed for a missing Gemini key and produced no PPTX/PNG output; this turn does not substitute text/chat tests for those missing capabilities. Previous evidence remains in the [live-validation journal](2026-09-10-live-validation.md).

The final mini3 binary embeds **web `39bbb1a` / core `b931bd2`**, PID **28709**, binary SHA-256 `19ba1b9580c68b07c8701db316f654fbe1f02ab5dbd05eb24cbb8d8381961279`, HTML SHA-256 `4415414f98c853420c5db6a4f98d71610f8c153942a5f4611f4b8c6685db5ba6`. HTTP readiness and exact served HTML were verified after startup. A readiness probe issued immediately after spawning initially saw connection refused; bounded startup readiness then passed before any browser check. The temporary candidate server was stopped after validation.

On that final binary, [deployment checks](evidence/2026-09-11-live/deployment-checks.json) pass **5/5 real core tests in 16.7s**, and Chromium/WebKit each pass all six original user turns on initial open and two reloads. [Three new actual weather cases](evidence/2026-09-11-live/weather-final.json) all pass source-URL matching with zero page errors. The [persisted citation check](evidence/2026-09-11-live/citation-final.json) reads the exact answer that failed before the fix: its source anchor has the correct URL and excludes punctuation on all three reads, and a real click requests that exact URL. [Inspected screenshot](evidence/2026-09-11-live/persisted-citation.png).

The source site responds to that Chromium navigation with **HTTP 301 to `/ok.html`**, which does not contain the Shanghai forecast. An initial assertion that the external destination would remain unchanged failed and was retained; the final check separates the application's correct link/request from the recorded external redirect. It does not claim the source website is readable in that browser. Tool-side retrieval succeeded separately. Web CI [run 34575831853](https://github.com/octos-org/octos-web/actions/runs/34575831853) and backend CI both pass. The completed 30-minute soak belongs to `ea8ce95` / `7590b73`; these later extraction, prompt and link changes have the separate checks above.
